#include "main.h"

/**
 * _isalpha - Function that checks for an alphabets.
 * @c: The character to check
 *
 * Return: 1 if c is a letter, 0 on failure.
 */
int _isalpha(int c)
{
	/* TODO: Write the _isalpha implementation */
	return (0);
}
