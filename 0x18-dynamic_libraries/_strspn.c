#include "main.h"

/**
 * _strspn - This gets the length of a determined substring
 * @s: first segment string
 * @accept: bytes in the string
 *
 * Return: the number of bytes in the initial segment.
 */
unsigned int _strspn(char *s, char *accept)
{
	/* Your code here */
	return (0);
}
