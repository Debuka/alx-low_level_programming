#include "main.h"

/**
 * _isdigit - Function that checks for a digit
 * @c: digit to check for.
 *
 * Return: 1 if c is a digit, 0 if it's not.
 */
int _isdigit(int c)
{
	/* Your code here */
	return (0);
}
