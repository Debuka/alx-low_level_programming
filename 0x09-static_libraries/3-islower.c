#include "main.h"

/**
 * _islower - lookss for lowercase char.
 * @c: the string to search through
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
